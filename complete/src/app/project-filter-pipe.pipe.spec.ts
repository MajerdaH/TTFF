import { ProjectFilterPipePipe } from './project-filter-pipe.pipe';

describe('ProjectFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
